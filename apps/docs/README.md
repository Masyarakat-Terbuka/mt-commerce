# @mt-commerce/docs

The mt-commerce documentation site, built with [Astro Starlight](https://starlight.astro.build/).

Run locally:

```bash
bun --filter '@mt-commerce/docs' dev
```

Bahasa Indonesia is the source of truth at `/`. English mirrors live at `/en/`.
