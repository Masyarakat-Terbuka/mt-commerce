---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { resolveLocale, localePath, type Locale } from "@/lib/i18n";

const locale: Locale = resolveLocale(Astro.currentLocale);
const heading = locale === "en" ? "Page not found" : "Halaman tidak ditemukan";
const body =
  locale === "en"
    ? "The page you were looking for is not here."
    : "Halaman yang Anda cari tidak tersedia.";
const home = locale === "en" ? "Back to home" : "Kembali ke beranda";
---

<BaseLayout title={`${heading} — mt-commerce`} description={body}>
  <section class="mx-auto max-w-[640px] px-4 py-32 text-center md:px-8">
    <p class="t-caption uppercase tracking-wide text-muted">404</p>
    <h1 class="mt-3 t-display text-fg">{heading}</h1>
    <p class="mt-4 t-body text-muted">{body}</p>
    <a href={localePath(locale, "/")} class="btn-primary mt-8 inline-flex">
      {home}
    </a>
  </section>
</BaseLayout>
