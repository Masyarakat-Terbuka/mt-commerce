# v0.1 Foundation

This document outlines the work for the first release of mt-commerce, **v0.1 Foundation**.

It is a working list of the pieces that need to come together for a small business to run a real store on the platform: products, checkout, payments, shipping, and notifications, all in Bahasa Indonesia, deployable on a modest server.

Each item is sized to be a self-contained piece of work, suitable for a single GitHub issue. The list is grouped into streams that can largely progress in parallel. Items within a stream are roughly ordered by dependency.

This document will be retired once the work is tracked in GitHub issues and projects. Until then, it serves as the shared map.

---

## Repository foundation

Setting up the project so contributors can find their footing.

- [ ] Create the `masyarakat-terbuka/mt-commerce` repository
- [x] Add `LICENSE` (MIT)
- [x] Add `README.md`, `PRODUCT.md`, `ARCHITECTURE.md`
- [x] Add `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md`
- [x] Add `.editorconfig`, `.gitignore`, `.bun-version`
- [x] Set up Bun workspaces
- [x] Set up shared TypeScript configuration
- [x] Set up shared ESLint and Prettier configuration
- [x] Set up commit hooks for linting and formatting
- [x] Set up commitlint with conventional commits
- [x] Add issue templates for bugs, features, and tasks
- [x] Add a pull request template
- [ ] Create the standard label set
- [ ] Set up the public roadmap project board
- [ ] Create the `v0.1 Foundation` milestone

---

## Local development

A developer should be running the full stack quickly after cloning.

- [x] Write the root `docker-compose.yml` with PostgreSQL and Redis
- [x] Write `.env.example` files for the API, admin, and storefront
- [x] Add a single command that starts everything for development
- [x] Document local setup in `apps/docs/src/content/docs/development/getting-started.mdx`

---

## Core API

The Hono application boots, has the conventions in place, and is ready to grow.

- [x] Initialize `apps/api` with TypeScript and Hono
- [x] Add `/health` and `/ready` endpoints
- [x] Configure structured logging with pino
- [x] Add request-ID middleware
- [x] Add error-handling middleware with the standard error shape
- [x] Set up Zod validation for request bodies and query parameters
- [x] Set up `@hono/zod-openapi` and serve the OpenAPI document
- [x] Set up Swagger UI in development
- [x] Add CORS middleware
- [x] Add rate-limiting middleware
- [x] Set up Vitest for unit and integration tests
- [x] Set up GitHub Actions for lint, typecheck, and test
- [x] Write a placeholder route and a test as a reference pattern

---

## Database and migrations

PostgreSQL is wired in, migrations work, and the foundation tables exist.

- [x] Add Drizzle ORM and `drizzle-kit`
- [x] Configure the Postgres connection pool
- [x] Implement the `Money` type and helper functions
- [x] Implement ULID-based ID generation with typed prefixes
- [x] Create the `audit_log` table and write helpers for recording state changes
- [x] Document the migration workflow in `apps/docs/src/content/docs/development/migrations.mdx`

---

## Authentication and authorization

Staff and customers can sign in. Roles take effect.

- [x] Integrate Better Auth with the Hono API
- [x] Define the `users` table for staff
- [x] Define the `customers` table
- [x] Implement password registration and login for staff
- [x] Implement password registration and login for customers
- [x] Implement session cookie handling (HTTP-only, secure)
- [x] Configure password hashing with Argon2id
- [x] Define the `roles` enum and assignment to staff users
- [x] Build the `requireAuth` and `requireRole` middlewares
- [x] Implement API keys with scopes for external services
- [x] Add tests for the full authentication and authorization flow
- [x] Document authentication in `apps/docs/src/content/docs/api-reference/authentication.mdx`

---

## Catalog

Products, variants, categories, and inventory.

- [x] Design and migrate `products`, `product_variants`, `categories`, `inventory_levels`
- [x] Implement the `Catalog` service interface
- [x] Implement product endpoints under `/admin/v1/products`
- [x] Implement variant endpoints
- [x] Implement category endpoints
- [x] Implement inventory adjustment endpoints with audit logging
- [x] Implement public product listing under `/storefront/v1/products` with pagination, filtering, and sorting
- [x] Implement public product detail endpoint
- [x] Add image upload (local disk for the first release)
- [x] Write integration tests for the catalog
- [x] Document the catalog module

---

## Customers

Customer accounts and addresses, with Indonesian address support.

- [x] Design and migrate `customers` and `customer_addresses` tables
- [x] Implement customer profile endpoints
- [x] Implement address endpoints
- [x] Add Indonesian address validation (provinsi, kota or kabupaten, kecamatan, kelurahan, postal code)
- [x] Seed the Indonesian administrative regions data
- [x] Document the customer module

---

## Cart and checkout

The cart works, and checkout has a clear state machine.

- [x] Design and migrate `carts` and `cart_items`
- [x] Implement cart creation, retrieval, and line management
- [x] Implement cart totals (subtotal, tax, shipping, total)
- [x] Support guest carts and customer carts
- [x] Design and document the checkout state machine
- [x] Implement checkout endpoints for each transition
- [x] Add idempotency support to checkout endpoints
- [x] Write integration tests covering the happy path and common edge cases
- [x] Document the cart and checkout flow

---

## Orders

Orders, fulfillment status, and history.

- [x] Design and migrate `orders`, `order_items`, `order_status_history`
- [x] Implement order creation from a completed checkout
- [x] Implement the order status state machine
- [x] Implement admin order list and detail endpoints
- [x] Implement customer order history endpoint
- [x] Emit order events
- [x] Add audit logging for order state changes
- [x] Write integration tests for the order lifecycle
- [x] Document the order module

---

## Payments

The payment provider interface, an in-memory test provider, and idempotent payment handling.

- [x] Design and migrate `payments` and `payment_attempts`
- [x] Define the `PaymentProvider` interface
- [x] Implement an in-memory test payment provider
- [x] Implement the payment service: initiate, capture, refund
- [x] Add the framework for verifying webhook signatures
- [x] Add an idempotency-key middleware for payment endpoints
- [x] Write integration tests using the test provider
- [x] Document the `PaymentProvider` interface

---

## Shipping

The shipping provider interface and the structure for rate calculation.

- [x] Design and migrate `shipping_methods` and `fulfillments`
- [x] Define the `ShippingProvider` interface
- [x] Implement a manual shipping provider with operator-defined flat rates
- [x] Implement shipping rate calculation in checkout
- [x] Implement fulfillment creation
- [x] Document the `ShippingProvider` interface

---

## Tax

Indonesian tax handling for the first release.

- [x] Design and migrate `tax_rates` and `tax_exemptions`
- [x] Implement Indonesian PPN calculation with a configurable rate
- [x] Apply tax in cart and order totals
- [x] Display tax-inclusive and tax-exclusive prices in the API
- [x] Document the tax module

---

## Notifications

Email and WhatsApp out, with adapters.

- [x] Define the `NotificationChannel` interface
- [x] Implement an SMTP email adapter, with a console fallback for development
- [x] Define the WhatsApp adapter interface
- [x] Implement notification templates for order confirmation, payment received, and shipping update
- [x] Wire notifications to events
- [x] Write integration tests with a mock channel
- [x] Document the `NotificationChannel` interface

---

## Plugins

Plugins can be installed and registered.

- [x] Define the `definePlugin` API and types
- [x] Implement the plugin loader, reading from `mt-commerce.config.ts`
- [x] Implement the first extension points: payment providers, shipping providers, notification channels, event listeners
- [x] Write the plugin author guide
- [x] Build a simple example plugin

---

## Midtrans payment plugin

The first real-world payment integration.

- [x] Create `packages/plugins/payment-midtrans`
- [x] Implement Midtrans Snap integration, covering QRIS, GoPay, OVO, DANA, virtual accounts, and cards
- [x] Handle Midtrans webhook events
- [x] Verify Midtrans webhook signatures
- [ ] Reconcile delayed payment confirmations
- [x] Write integration tests against the Midtrans sandbox
- [x] Document setup of the Midtrans plugin

---

## Biteship shipping plugin

The first real-world shipping integration.

- [x] Create `packages/plugins/shipping-biteship`
- [x] Implement Biteship rate calculation across major Indonesian couriers
- [x] Implement Biteship order creation
- [x] Handle Biteship tracking webhooks
- [x] Add cash-on-delivery support
- [x] Write integration tests against the Biteship sandbox
- [x] Document setup of the Biteship plugin

---

## WhatsApp notification plugin

Real WhatsApp delivery for order events.

- [x] Create `packages/plugins/notification-whatsapp`
- [x] Implement the WhatsApp Business API adapter
- [x] Implement template messages for order confirmation, payment received, and shipping update
- [x] Document setup of the WhatsApp plugin

---

## SDK

A typed client for talking to the API.

- [x] Initialize `packages/sdk`
- [x] Generate types from the API's OpenAPI document
- [x] Implement a fetch-based client with typed methods
- [x] Add helpers for authentication, error handling, and idempotency
- [x] Document SDK usage

---

## Admin app

A complete reference admin for shop owners.

- [x] Initialize `apps/admin` with Vite, React, and TypeScript
- [x] Set up TanStack Router and TanStack Query
- [x] Set up shadcn/ui and Tailwind CSS
- [x] Set up internationalization, with Bahasa Indonesia as the default
- [x] Build the login page
- [x] Build the main layout
- [x] Build the dashboard with overview, recent orders, and sales summary
- [x] Build the products page (list, search, filter)
- [x] Build the product editor (create, edit, variants, images)
- [x] Build the categories page
- [x] Build the orders page
- [x] Build the order detail page
- [x] Build the customers page
- [x] Build the customer detail page
- [x] Build the inventory page
- [x] Build the settings pages
- [x] Build staff and roles management
- [ ] Add empty, loading, and error states everywhere
- [ ] Write component tests for the most important screens

---

## Storefront

A complete, shippable reference storefront.

- [x] Initialize `apps/storefront` with Astro and TypeScript
- [x] Set up `@astrojs/react` for interactive islands
- [x] Set up Tailwind CSS
- [x] Set up internationalization, with Bahasa Indonesia as the default
- [x] Wire up the SDK as the API client
- [x] Build the home page
- [x] Build the product listing page with filters and pagination
- [x] Build the product detail page, with the variant selector as a React island
- [x] Build the cart
- [x] Build the checkout flow (address, shipping, payment, review)
- [x] Build the order confirmation page
- [x] Build the customer account pages
- [x] Implement search using Postgres full-text
- [x] Add SEO essentials: sitemap, structured data, Open Graph tags
- [x] Apply Rupiah formatting throughout
- [ ] Run a Lighthouse audit and address findings, with mobile as the priority
- [ ] Test on real Android devices

---

## Deployment

A merchant or developer can put the platform on a server in a sitting.

- [x] Write Dockerfiles for the API, admin, and storefront
- [x] Write a production `docker-compose.yml`
- [x] Write deployment guides for Hetzner, Biznet Gio, IDCloudHost, and a generic Linux VPS
- [x] Document a Postgres backup and restore strategy
- [x] Document basic firewall and SSL setup
- [ ] Run an end-to-end deployment test on a real server

---

## Documentation

The documentation is part of the product.

- [x] Set up the documentation site
- [x] Write the introduction and concept pages
- [x] Write the quickstart guide
- [x] Write the architecture overview for newcomers
- [x] Generate the API reference from the OpenAPI document (overview, authentication, and an interactive Scalar explorer backed by `apps/docs/public/openapi.json` — refreshed via `bun --filter @mt-commerce/api dump:openapi`)
- [x] Write the admin user guide for shop owners, in Bahasa Indonesia
- [x] Write the plugin author guide
- [x] Translate the deployment, plugin author guide, API reference, and per-plugin pages into Bahasa Indonesia (admin/intro/concepts already shipped in ID)
- [ ] Set up the documentation domain

---

## Quality

A short list of checks before tagging the release.

- [ ] All CI checks pass on the main branch
- [ ] An end-to-end run of the full happy path: create a product, browse the store, add to cart, check out, pay through the Midtrans sandbox, see the order in the admin, mark it fulfilled, customer receives a WhatsApp message
- [ ] Manual checks on Chrome, Safari, and Firefox
- [ ] Manual checks on a real Android phone over a typical mobile connection
- [ ] Lighthouse audit on the storefront, mobile profile
- [ ] A security review covering secrets, rate limits, idempotency, webhook signatures, and input validation
- [ ] A baseline load test of the API
- [ ] A backup and restore drill on the production-like setup
- [ ] All v0.1 ADRs written and merged

---

## Release

Quiet, confident, public.

- [ ] Tag `v0.1.0` and publish a release with detailed notes
- [ ] Open the GitHub Discussions board for community questions
- [ ] Prepare the next milestone

---

## Notes on this list

The list is ambitious. Some streams may grow, some may shrink, and some items may move to a later release as the work meets reality. The shape of the foundation is what matters; the order and scope of the items are not fixed.

If the list feels too large, that is honest information about the size of building a commerce platform from scratch. We err on the side of writing things down so the work is visible, then deciding together what fits in the first release and what waits.
